<Window x:Class="SKAuto.UI.Views.VehicleEditWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Vehicle" Height="350" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Chassis -->
        <Label Grid.Row="0" Grid.Column="0" Content="Chassis Number:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Vehicle.ChassisNumber}" Margin="5" Padding="3"/>

        <!-- Make -->
        <Label Grid.Row="1" Grid.Column="0" Content="Make:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Vehicle.Make}" Margin="5" Padding="3"/>

        <!-- Model -->
        <Label Grid.Row="2" Grid.Column="0" Content="Model:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Vehicle.Model}" Margin="5" Padding="3"/>

        <!-- Year -->
        <Label Grid.Row="3" Grid.Column="0" Content="Year:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding Vehicle.Year, TargetNullValue=''}" Margin="5" Padding="3"/>

        <!-- Registration -->
        <Label Grid.Row="4" Grid.Column="0" Content="Registration:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="4" Grid.Column="1" Text="{Binding Vehicle.Registration}" Margin="5" Padding="3"/>

        <!-- Is Active -->
        <Label Grid.Row="5" Grid.Column="0" Content="Is Active:" VerticalAlignment="Center"/>
        <CheckBox Grid.Row="5" Grid.Column="1" IsChecked="{Binding Vehicle.IsActive}" Margin="5" VerticalAlignment="Center"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Save" Command="{Binding SaveCommand}" Width="80" Height="30" Margin="5" Background="#4CAF50" Foreground="White"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" Width="80" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>