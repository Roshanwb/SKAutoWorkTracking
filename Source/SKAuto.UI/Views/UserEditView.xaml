<Window x:Class="SKAuto.UI.Views.UserEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="480" Width="500"
        WindowStartupLocation="CenterOwner"
        Background="#F5F5F5">
    <Window.Resources>
        <Style x:Key="FormLabel" TargetType="Label">
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="Foreground" Value="#2C3E50"/>
            <Setter Property="Margin" Value="0,5,0,0"/>
        </Style>
        <Style x:Key="FormTextBox" TargetType="TextBox">
            <Setter Property="Height" Value="30"/>
            <Setter Property="BorderBrush" Value="#BDC3C7"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5,0"/>
            <Setter Property="Margin" Value="0,2,0,10"/>
        </Style>
    </Window.Resources>

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <TextBlock Grid.Row="0" Grid.ColumnSpan="2" Text="{Binding WindowTitle}" FontSize="16" FontWeight="Bold" Foreground="#2C3E50" Margin="0,0,0,10"/>

        <!-- Username -->
        <Label Grid.Row="1" Grid.Column="0" Content="Username:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="5" Padding="3" BorderBrush="#dee2e6" IsEnabled="{Binding IsUsernameEditable}"/>

        <!-- Email (Add/Edit) -->
        <Label Grid.Row="2" Grid.Column="0" Content="Email:" VerticalAlignment="Center" Visibility="{Binding ShowEmailField, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}" Margin="5" Padding="3" BorderBrush="#dee2e6" IsEnabled="{Binding IsEmailEditable}" Visibility="{Binding ShowEmailField, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Password (varies by mode) -->
        <Label Grid.Row="3" Grid.Column="0" Content="{Binding PasswordLabel}" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="3" Grid.Column="1" Margin="5" Padding="3" BorderBrush="#dee2e6" PasswordChanged="PasswordBox_PasswordChanged"/>

        <!-- New Password (only for ChangePassword) -->
        <Label Grid.Row="4" Grid.Column="0" Content="{Binding NewPasswordLabel}" VerticalAlignment="Center" Visibility="{Binding ShowNewPasswordField, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <PasswordBox x:Name="NewPasswordBox" Grid.Row="4" Grid.Column="1" Margin="5" Padding="3" BorderBrush="#dee2e6" PasswordChanged="NewPasswordBox_PasswordChanged" Visibility="{Binding ShowNewPasswordField, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Confirm Password -->
        <Label Grid.Row="5" Grid.Column="0" Content="{Binding ConfirmPasswordLabel}" VerticalAlignment="Center" Visibility="{Binding ShowConfirmPassword, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="5" Grid.Column="1" Margin="5" Padding="3" BorderBrush="#dee2e6" PasswordChanged="ConfirmPasswordBox_PasswordChanged" Visibility="{Binding ShowConfirmPassword, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Role (Admin) -->
        <Label Grid.Row="6" Grid.Column="0" Content="Role:" VerticalAlignment="Center" Visibility="{Binding ShowAdminFields, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <ComboBox Grid.Row="6" Grid.Column="1" ItemsSource="{Binding RoleValues}" SelectedItem="{Binding SelectedRole}" Margin="5" Height="28" Width="120" HorizontalAlignment="Left" VerticalAlignment="Center" Visibility="{Binding ShowAdminFields, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Active (Admin) -->
        <Label Grid.Row="7" Grid.Column="0" Content="Active:" VerticalAlignment="Center" Visibility="{Binding ShowAdminFields, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <CheckBox Grid.Row="7" Grid.Column="1" IsChecked="{Binding IsActive}" Margin="5" VerticalAlignment="Center" Visibility="{Binding ShowAdminFields, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Verification Code (ResetPassword) -->
        <Label Grid.Row="8" Grid.Column="0" Content="Verification Code:" VerticalAlignment="Center" Visibility="{Binding ShowVerificationCodeField, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <TextBox Grid.Row="8" Grid.Column="1" Text="{Binding VerificationCode, UpdateSourceTrigger=PropertyChanged}" Margin="5" Padding="3" BorderBrush="#dee2e6" Visibility="{Binding ShowVerificationCodeField, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="9" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="{Binding ActionButtonText}" Command="{Binding ActionCommand}" 
                    Style="{StaticResource ActionButton}" Background="#4CAF50"
                    Width="80" Height="30" Margin="5"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" 
                    Style="{StaticResource ActionButton}" Background="#6c757d"
                    Width="80" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>