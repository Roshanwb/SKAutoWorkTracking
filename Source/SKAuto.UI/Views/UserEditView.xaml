<Window x:Class="SKAuto.UI.Views.UserEditView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="{Binding WindowTitle}" Height="400" Width="500"
        WindowStartupLocation="CenterOwner">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Username -->
        <Label Grid.Row="0" Grid.Column="0" Content="Username:" VerticalAlignment="Center"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Margin="5" Padding="3" BorderBrush="#dee2e6" IsEnabled="{Binding IsUsernameEditable}"/>

        <!-- Password -->
        <Label Grid.Row="1" Grid.Column="0" Content="{Binding PasswordLabel}" VerticalAlignment="Center"/>
        <PasswordBox x:Name="PasswordBox" Grid.Row="1" Grid.Column="1" Margin="5" Padding="3" BorderBrush="#dee2e6" PasswordChanged="PasswordBox_PasswordChanged"/>

        <!-- Confirm Password (visible for Add/ChangePassword) -->
        <Label Grid.Row="2" Grid.Column="0" Content="{Binding ConfirmPasswordLabel}" VerticalAlignment="Center" Visibility="{Binding ShowConfirmPassword, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <PasswordBox x:Name="ConfirmPasswordBox" Grid.Row="2" Grid.Column="1" Margin="5" Padding="3" BorderBrush="#dee2e6" PasswordChanged="ConfirmPasswordBox_PasswordChanged" Visibility="{Binding ShowConfirmPassword, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Role (Admin only) -->
        <Label Grid.Row="3" Grid.Column="0" Content="Role:" VerticalAlignment="Center" Visibility="{Binding ShowAdminFields, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <ComboBox Grid.Row="3" Grid.Column="1" ItemsSource="{Binding RoleValues}" SelectedItem="{Binding SelectedRole}" Margin="5" Height="30" Width="120" HorizontalAlignment="Left" Visibility="{Binding ShowAdminFields, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Active (Admin only) -->
        <Label Grid.Row="4" Grid.Column="0" Content="Active:" VerticalAlignment="Center" Visibility="{Binding ShowAdminFields, Converter={StaticResource BooleanToVisibilityConverter}}"/>
        <CheckBox Grid.Row="4" Grid.Column="1" IsChecked="{Binding IsActive}" Margin="5" VerticalAlignment="Center" Visibility="{Binding ShowAdminFields, Converter={StaticResource BooleanToVisibilityConverter}}"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="7" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="5">
            <Button Content="Save" Command="{Binding SaveCommand}" 
                    Style="{StaticResource ActionButton}" Background="#4CAF50"
                    Width="80" Height="30" Margin="5"/>
            <Button Content="Cancel" Command="{Binding CancelCommand}" 
                    Style="{StaticResource ActionButton}" Background="#6c757d"
                    Width="80" Height="30" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>