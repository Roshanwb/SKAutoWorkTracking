<Window x:Class="SKAuto.UI.Views.GoogleDriveSettingsView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Google Drive Settings" Height="450" Width="500"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="120"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>

        <Label Grid.Row="0" Grid.Column="0" Content="Client ID:"/>
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding ClientId}" Margin="5"/>
        <Button Grid.Row="0" Grid.Column="2" Content="?" ToolTip="Get from Google Cloud Console"/>

        <Label Grid.Row="1" Grid.Column="0" Content="Client Secret:"/>
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding ClientSecret}" Margin="5"/>

        <Label Grid.Row="2" Grid.Column="0" Content="User Email:"/>
        <TextBox Grid.Row="2" Grid.Column="1" Text="{Binding UserEmail}" Margin="5"/>

        <Label Grid.Row="3" Grid.Column="0" Content="Folder ID:"/>
        <TextBox Grid.Row="3" Grid.Column="1" Text="{Binding FolderId}" Margin="5"/>
        <Button Grid.Row="3" Grid.Column="2" Content="Browse" Command="{Binding BrowseFolderCommand}" Margin="5"/>

        <StackPanel Grid.Row="4" Grid.ColumnSpan="3" Orientation="Horizontal" HorizontalAlignment="Center" Margin="5">
            <Button Content="Authenticate" Command="{Binding AuthenticateCommand}" Margin="5" Padding="10,5"/>
            <Button Content="Test Connection" Command="{Binding TestConnectionCommand}" Margin="5" Padding="10,5" IsEnabled="{Binding IsConnected}"/>
            <Button Content="Save Settings" Command="{Binding SaveSettingsCommand}" Margin="5" Padding="10,5"/>
        </StackPanel>

        <Border Grid.Row="5" Grid.ColumnSpan="3" Background="#f0f0f0" Padding="10" Margin="5">
            <StackPanel>
                <TextBlock Text="Status:" FontWeight="Bold"/>
                <TextBlock Text="{Binding StatusMessage}" TextWrapping="Wrap"/>
                <TextBlock Text="Last Sync:" FontWeight="Bold" Margin="0,5,0,0"/>
                <TextBlock Text="{Binding LastSync, StringFormat='{}{0:dd/MM/yyyy HH:mm}'}"/>
            </StackPanel>
        </Border>

        <Button Grid.Row="6" Grid.ColumnSpan="3" Content="Sync Now" Command="{Binding SyncNowCommand}" 
                HorizontalAlignment="Center" Width="100" Margin="5" Padding="10,5"/>
    </Grid>
</Window>