<Window x:Class="SKAuto.UI.Views.VehicleManagementView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Manage Vehicles" Height="500" Width="900"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <DataGrid Grid.Row="0" ItemsSource="{Binding Vehicles}" SelectedItem="{Binding SelectedVehicle}"
                  AutoGenerateColumns="False" IsReadOnly="True" Margin="0,0,0,10">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Chassis" Binding="{Binding ChassisNumber}" Width="150"/>
                <DataGridTextColumn Header="Make" Binding="{Binding Make}" Width="80"/>
                <DataGridTextColumn Header="Model" Binding="{Binding Model}" Width="100"/>
                <DataGridTextColumn Header="Year" Binding="{Binding Year}" Width="60"/>
                <DataGridTextColumn Header="Reg" Binding="{Binding Registration}" Width="80"/>
                <DataGridCheckBoxColumn Header="Active" Binding="{Binding IsActive}" Width="60"/>
                <DataGridTextColumn Header="Notes" Binding="{Binding Notes}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Add" Command="{Binding AddVehicleCommand}" Width="80" Margin="5"/>
            <Button Content="Edit" Command="{Binding EditVehicleCommand}" Width="80" Margin="5"/>
            <Button Content="Delete" Command="{Binding DeleteVehicleCommand}" Width="80" Margin="5"/>
            <Button Content="Close" Command="{Binding CloseCommand}" Width="80" Margin="5"/>
        </StackPanel>
    </Grid>
</Window>